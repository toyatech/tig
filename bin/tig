#!/usr/bin/env node

var tig = require('../')
  , program = require('commander');

program
  .version(tig.VERSION)
  .command('hash-object', 'Compute object ID and optionally creates a blob from a file')
  .parse(process.argv);

